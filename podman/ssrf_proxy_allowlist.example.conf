# Optional forward-proxy rule for an MCP client network. Include this before
# the proxy's final `http_access deny all` rule. Allow only the synthetic
# service name used by the deployment; do not allow broad private networks.
acl arcsuite_mcp dstdomain arcsuite-mcp
http_access allow arcsuite_mcp
